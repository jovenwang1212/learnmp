<view class="list">
  <view class="hero" wx:for="{{heroList}}" wx:key="id" bindtap="toDetail" data-id="{{item.id}}">
    <image src="{{item.iconUrl}}"></image>
    <text>{{item.name}}</text>
  </view>
</view>